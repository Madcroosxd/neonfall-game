name: NEONFALL v3 gameplay upgrade
on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/v3-gameplay-upgrade.yml'
permissions:
  contents: write
jobs:
  upgrade:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build v3
        shell: bash
        run: |
          cat > index.html <<'EOF'
          <!doctype html>
          <html lang="tr"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>NEONFALL: XENO HUNT v